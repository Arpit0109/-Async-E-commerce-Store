<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <a
    href="javascript:history.back()"
    class="fixed top-10 right-10 bg-red-500 text-white text-xl font-semibold px-6 py-2 rounded-full shadow-lg hover:bg-red-600 transition duration-200 z-10"
  >
    ← Back
  </a>

  <% user.cart.forEach((item)=>{ %>

  <body class="font-['helvetica_now_display']">
    <nav class="w-full fixed top-0 left-0 px-5 py-3">
      <h3 class="text-xl">Async</h3>
    </nav>
    <div class="w-full h-screen flex items-start px-20 py-20 gap-10 relative">
      <a
        href="/remove/<%= item._id %>"
        class="absolute top-[300px] right-10 bg-red-500 text-white text-lg font-medium px-5 py-2 rounded-full shadow-md hover:bg-red-600 transition duration-200"
      >
        🗑️ Remove Item
      </a>
      <div class="w-[30%] rounded-md overflow-hidden">
        <div
          class="w-full flex justify-center items-center h-80 bg-[<%= item.bgcolor %>]"
        >
          <img
            class="h-[20rem]"
            src="data:image/jpeg;base64,<%= item.image.toString('base64') %>"
            alt=""
          />
        </div>
        <div
          class="w-full flex justify-between px-5 py-4 bg-[<%= item.panelcolor %>]"
        >
          <h3 class="text-2xl text-[<%= item.textcolor %>]">
            <%= item.name %>
          </h3>
          <div class="flex items-center gap-2">
            <i
              class="w-7 h-7 bg-white flex rounded-full items-center justify-center ri-add-line"
            ></i>
            <div class="px-2 py-1 rounded-md bg-white text-black">01</div>
            <i
              class="w-7 h-7 bg-white flex rounded-full items-center justify-center ri-subtract-line"
            ></i>
          </div>
        </div>
        <div
          class="text-white flex items-center justify-between px-5 py-3 bg-[<%= item.textcolor %>]"
        >
          <h4 class="text-lg">Net Total</h4>
          <h2 class="text-lg">
            ₹ <%= Number(item.price) + 40 - Number(item.discount) %>
          </h2>
        </div>
      </div>
      <div class="w-[70%]">
        <h3 class="text-xl">Price Breakdown</h3>
        <div class="px-10 mt-5">
          <div class="flex mt-2">
            <h4 class="w-1/3">Total MRP</h4>
            <h4>₹ <%= item.price %></h4>
          </div>
          <div class="flex mt-2">
            <h4 class="w-1/3">Discount on MRP</h4>
            <h4>₹ <%= Number(item.discount) %></h4>
          </div>
          <div class="flex mt-2">
            <h4 class="w-1/3">Platform Fee</h4>
            <h4>₹ 40</h4>
          </div>
          <div class="flex mt-2">
            <h4 class="w-1/3">Shipping Fee</h4>
            <h4>FREE</h4>
          </div>
        </div>
        <div class="w-full h-[1px] bg-black mt-10"></div>
        <div class="flex mt-5">
          <h3 class="w-1/3 text-xl">Total Amount</h3>
          <h3 class="font-semibold text-xl text-green-600">
            ₹ <%= Number(item.price) + 40 - Number(item.discount) %>
          </h3>
        </div>
      </div>
    </div>
  </body>
  <% }) %>
</html>
